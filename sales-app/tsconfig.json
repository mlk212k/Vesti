{
  "compilerOptions": {
    "target": "ES2022",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    // Les scripts (seed) tournent via `node --experimental-strip-types`, qui
    // exige l'extension .ts dans les imports. Sans cette option, TypeScript
    // la refuse — et le seed ne pourrait pas partager de code avec l'app.
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    // Les fonctions Edge tournent sur Deno, pas sur Node : elles importent
    // en `jsr:` / `npm:` et utilisent le global `Deno`. Les vérifier avec la
    // configuration de l'app produirait dix erreurs qui n'en sont pas.
    "supabase/functions"
  ]
}
